# GraphQL-with-postgres-mongo-example
GraphQL with postgres and  mongo db example

simple example of GraphQL with mongo and postgres db
 
### just play with it

- `npm i`

- `npm start`

🚀  Server ready at http://localhost:4000/
